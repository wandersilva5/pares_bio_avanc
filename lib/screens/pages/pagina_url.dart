import 'package:flutter/material.dart';

class PaginaUrl extends StatelessWidget {
  const PaginaUrl({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
